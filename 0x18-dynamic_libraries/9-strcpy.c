#include "main.h"
#include <string.h>

/**
 * _strcpy -  copies the string pointed to by src, to dest
 * @dest: the destination pointer
 * @src: the source string pointer
 * Return: pointer for the destination
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
