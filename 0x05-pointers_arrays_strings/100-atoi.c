#include "main.h"
#include <stdlib.h>

/**
 * _atoi - converts a string to an integer
 * @s: pointer for the string argument
 * Return: the converted integer
 */
int _atoi(char *s)
{
	int n;
	n = atoi(s);
	return (n);
}
